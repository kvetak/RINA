Small world graph:
The main files of this program are:

SWgraph.cc: generate a small world like graph
smallworld.cc: generate the hierarchical small world graph
CW.cc/.h: the implementation of the chinese whisper clustering algorithm

To compile the small world program:  g++ SWgraph.cc -o SWgraph
To run the hierarchical small world program: ./SWgraph

To compile the hierarchical small world program:  g++ smallworld.cc CW.cc -o smallworld
To run the hierarchical small world program: the input are three files generated by the SWgraph program (the graph, nodes and phy files)
./smallworld swgraph.txt swnodes.txt swphy.txt 
